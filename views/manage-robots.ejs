<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>
        Manage robots
    </title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="styles.css">
</head>

<body>
    <div class="container mw-540 p-3">
        <h3 class="mt-4">🤖 Telepresence Robot Portal</h3>
        <h5 class="mb-5 py-2">Admin dashboard</h5>
    </div>

    <div class="container-fluid bg-dark">
        <div class="container mw-540 px-3 py-2">
            <div class=" d-flex">
                <div class="d-flex py-2 me-auto">
                    <h6 class="my-auto text-white">Welcome,<br><b>
                            <%= email %>!
                        </b></h6>
                </div>
                <div class="d-flex py-2">
                    <form method="GET" action="/admin-dashboard">
                        <button type="submit" class="btn btn-light">⬅️ Back</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div class="container mw-540 p-3">
        <h6 class="mx-3 mt-3">Active robots</h6>
        <% Object.keys(activeRobots).forEach(function(uuid) { %>
        <div class="d-flex bg-light text-black mx-2 mt-3 mb-3 px-4 py-3 rounded">
            <div class="d-flex me-auto">
                <h6 class="my-auto"><%= activeRobots[uuid].name %> — <%= activeRobots[uuid].location %><br><b><%= activeRobots[uuid].private %></b></h6>
            </div>
            <form method="POST" action="/delete-robot/<%= uuid %>">
                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
            </form>
        </div>
        <% }) %>
        <h6 class="mx-3 mt-3">Add new robot</h6>
        <form method="POST" action="/add-robot">
            <div class="form-floating w-50 mt-3 m-2">
                <input name="name" type="text" class="form-control" id="name" placeholder="Robert"
                    required>
                <label for="name">Robot name</label>
            </div>
            <div class="form-floating w-50 m-2">
                <input name="location" type="text" class="form-control" id="location" placeholder="London, UK"
                    required>
                <label for="location">Location description</label>
            </div>
            <button type="submit" class="btn m-2 btn btn-dark">Add new robot</button>
        </form>
        
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"></script>
</body>

</html>